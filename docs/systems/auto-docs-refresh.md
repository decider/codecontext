---
name: auto-docs-refresh
kind: hook
summary: >
  Per-PR documentation refresh driven by Claude Code hooks. A PostToolUse(Bash)
  hook detects PR-opening/updating commands and commits regenerated AUTO_DOCS +
  docs/systems back onto the branch; a companion PreCompact/SessionStart hook
  wipes inject-dedup state so freshly-merged docs re-enter the model's context.
globs:
  - hooks/auto-docs-refresh.mjs
  - hooks/auto-docs-refresh.test.mjs
  - hooks/reset-inject-dedup.mjs
  - hooks/reset-inject-dedup.test.mjs
status: active
quality_score: 6.9
quality_verdict: revise
quality_input_hash: 7344ebdbdf5f9cd3
---
<!-- auto-generated by systems-registry — do not hand-edit. Run `tools/systems-registry/cli.mjs run` (full sweep) or `tools/systems-registry/cli.mjs refresh --changed <files>` (incremental) to refresh. -->


# auto-docs-refresh

## What it does

`hooks/auto-docs-refresh.mjs` fires on every `PostToolUse(Bash)` call but immediately exits when `AUTODOC_SKIP=1`; otherwise it reads stdin, gates on `tool_name === 'Bash'` and `isPrCommand()` (`gh pr create` or a feature-branch `git push`), and requires a non-empty, non-HEAD, non-main branch before doing real work. Its fast `main()` does cheap marker/lock checks then spawns `runDetachedWorker()`, which fetches, diffs against `origin/main`, converts changed file paths to unique parent directories via `changedDirs()`, runs docgen with `--until-done --parallel 4 --changed <dirs>` (directory-scoped) and sysreg with raw file paths (file-scoped), and — if any `docPathsFromStatus()` files changed — commits with `--no-verify` (to avoid hook re-entrancy on the docs commit itself) and pushes them onto the branch so they ride the squash-merge into main. `hooks/reset-inject-dedup.mjs` runs on `PreCompact`/`SessionStart` and calls `resetDedup()` to delete per-session dedup files, so the next file Read re-injects the now-fresh README and system diagram.

## The loop

```mermaid
flowchart TB
  subgraph FastHook["Fast hook path — main()"]
    SK{"AUTODOC_SKIP=1?"}
    B["read stdin: verify tool_name=Bash,\nisPrCommand, non-empty non-HEAD non-main branch"]
    MK{marker == HEAD sha?}
    LK{"lock exists and fresh?"}
    REAP[rmSync stale lock]
    CLAIM["claim lock atomically O_CREAT O_EXCL\n+ spawn detached worker"]
  end
  subgraph Worker["Detached worker — AUTODOC_SKIP=1"]
    FD[fetch + diff branch vs origin/main]
    CD["changedDirs: file paths to unique parent dirs"]
    RF["docgen --until-done --parallel 4 --changed dirs\nsysreg refresh --changed raw files"]
    CH{doc paths changed?}
    CP["git commit --no-verify + push docs"]
    WM["write HEAD sha marker\nlock released unconditionally in finally"]
  end
  SK -->|yes| NOOP((exit 0 — no-op))
  SK -->|no| B
  B -->|any guard fails| NOOP
  B -->|all pass| MK
  MK -->|yes — already refreshed| NOOP
  MK -->|no| LK
  LK -->|yes — fresh lock| NOOP
  LK -->|no — stale| REAP
  REAP --> CLAIM
  CLAIM -->|"openSync throws — race lost"| NOOP
  CLAIM -->|worker pid| FD
  FD -->|changed file paths| CD
  CD -->|dirs for docgen, raw files for sysreg| RF
  RF -->|git status --porcelain| CH
  CH -->|no| WM
  CH -->|yes| CP
  CP -->|pushed branch| WM
  WM -.->|squash-merge to main| NEXT((next session reads fresh docs))
```

## Subflow: reset-inject-dedup

Independent of the refresh flow: on compaction or session start, wipe the dedup files that otherwise falsely report "already injected" after the diagrams are evicted from the model's context.

```mermaid
flowchart TB
  H[PreCompact / SessionStart payload]
  SID[extract session_id + hook_event_name]
  CAND["candidatePaths: raw sid for sysreg,\nsanitized sid for docgen, across tmpdir + /tmp"]
  RM[resetDedup: unlink each existing dedup file]
  T["writeEvent reset + writeIndexEntry session-reset"]
  H -->|session_id| SID
  SID -->|raw + sanitized id| CAND
  CAND -->|candidate path list| RM
  RM -->|removed paths| T
  RM -.->|next file Read re-injects README + diagram| NEXT((Claude turn))
```

## Anchors

- `hooks/auto-docs-refresh.mjs` — the PostToolUse hook; `main()` fast path + `runDetachedWorker()` slow path.
- `hooks/auto-docs-refresh.mjs:isPrCommand` — the gate deciding whether a Bash command warrants a refresh.
- `hooks/auto-docs-refresh.mjs:changedDirs` — converts `git diff --name-only` file paths to unique parent directories (root-level files map to `"."`); bridges the diff output to docgen's `--changed <dirs>` argument and separates directory-scoped (docgen) from file-scoped (sysreg) invocation.
- `hooks/auto-docs-refresh.mjs:docgenCandidates / sysregCandidates` — portability: resolves host-repo vendored tools or the bundled siblings beside this hook, whichever exist first.
- `hooks/reset-inject-dedup.mjs:candidatePaths / resetDedup` — the dedup-file reset across both inject scripts' naming conventions and both tmpdir paths.

## Closing arrow

The worker commits the regenerated `AUTO_DOCS.md` + `docs/systems` onto the feature branch and pushes (`CP` node), so the docs ride the eventual squash-merge into `main`. A future clone/session reads the merged-fresh docs, and after compaction `reset-inject-dedup` clears the dedup state so those docs re-enter the model's context on the next file Read — closing the loop back to a Claude turn.

## Log flow

| Marker / Event | Emitter (file:fn) | Fields | Consumer | Lands in |
|---|---|---|---|---|
| `refresh-spawned` | `auto-docs-refresh.mjs:main` (`_telemetry`) | branch, head, root, worker_pid | telemetry reader | `telemetry/writer.mjs` writeEvent |
| `refresh-skipped` | `auto-docs-refresh.mjs:main` (`_telemetry`) | reason (not-a-git-repo / main-branch / already-refreshed / lock-held / lock-race-lost), head, root | telemetry reader | `telemetry/writer.mjs` writeEvent |
| `reset` | `reset-inject-dedup.mjs:main` (`writeEvent`) | reason (hook_event_name), dedup_files_removed, removed_paths | telemetry reader | `telemetry/writer.mjs` writeEvent |
| `session-reset` | `reset-inject-dedup.mjs:main` (`writeIndexEntry`) | reason, dedup_files_removed | cross-session rollup | `telemetry/writer.mjs` index |

## Data tables

| Table / File | Schema / Migration | Writer (file:fn) | Reader (file:fn) | Purpose |
|---|---|---|---|---|
| `<git-dir>/auto-docs-refresh.sha` | n/a (raw sha string) | `auto-docs-refresh.mjs:runDetachedWorker` — writes `head` (pre-commit sha) when no docs changed; writes `newHead` (post-commit sha) when docs were committed and pushed | `auto-docs-refresh.mjs:main` (marker == HEAD check) | Per-HEAD dedup so create-then-push won't double-run |
| `<git-dir>/auto-docs-refresh.lock` | n/a (`pid head`) | `auto-docs-refresh.mjs:main` (`openSync 'wx'`, O_CREAT O_EXCL) | `auto-docs-refresh.mjs:main` (lock-held check); worker refreshes mtime; released unconditionally in `finally` | Atomic mutual exclusion of overlapping refreshes |
| `<tmpdir\|/tmp>/systems-registry-injected-<sid>.json` | n/a | systems-registry `inject.mjs` (external) | `reset-inject-dedup.mjs:resetDedup` (unlink) | Per-session "already injected" flag for system pages |
| `<tmpdir\|/tmp>/docgen-injected-<safe>.json` | n/a | docgen `inject-readme-context.mjs` (external) | `reset-inject-dedup.mjs:resetDedup` (unlink) | Per-session "already injected" flag for READMEs |
| `/tmp/auto-docs-refresh.log` | append-only text | both phases (`log()`, worker stdio) | operator | Timeline of spawns/skips/worker output |

## Invariants

- Both hooks ALWAYS exit 0 — they never block a Bash call, compaction, or session start (`auto-docs-refresh.mjs` header; `reset-inject-dedup.mjs` `.finally(() => process.exit(0))`).
- Never touches `main`/`master`: `main()` exits early when `!branch || branch === 'HEAD' || branch === 'main' || branch === 'master'`.
- Lock claim is atomic via `openSync(lock, 'wx')` (O_CREAT O_EXCL) — only one of two racing `main()` calls wins; the loser logs `lock-race-lost` and exits 0.
- Re-entry guard: the worker is spawned with `AUTODOC_SKIP=1`, and `main()` returns immediately on that env var — before reading stdin, before any I/O.
- Timeouts: `REFRESH_TIMEOUT_MS = 8 * 60 * 1000` SIGKILLs a hung child; `LOCK_STALE_MS = 20 * 60 * 1000` reaps an abandoned lock by mtime.

## Failure modes

- Worktree `.git` is a file (linked worktree), so the marker/lock must land in `gitDir()`'s `rev-parse --git-dir` location, not `<root>/.git` — else writes fail with "not a directory".
- A crashed worker that never reached its `finally` block leaves the marker unwritten; the next push re-runs (correct) but a stale lock blocks until `LOCK_STALE_MS` elapses.
- After compaction without `reset-inject-dedup` firing, dedup files would falsely report "already injected" and fresh diagrams would silently never re-enter context.
- `docgenCandidates`/`sysregCandidates` resolving to neither a host-repo copy nor a bundled sibling makes the refresh a silent no-op (no tools to run).

## Where to start reading

1. `hooks/auto-docs-refresh.mjs:isPrCommand` — the gate that decides if anything happens at all.
2. `hooks/auto-docs-refresh.mjs:main` — fast PostToolUse path: AUTODOC_SKIP guard first, then marker/lock checks, then detached spawn.
3. `hooks/auto-docs-refresh.mjs:changedDirs` — the bridge between diff output and tool invocation scope; docgen receives directories, sysreg receives raw file paths.
4. `hooks/auto-docs-refresh.mjs:runDetachedWorker` — the slow path: fetch, diff, docgen + sysreg, `--no-verify` commit, push; marker written at two different shas depending on whether docs changed.
5. `hooks/reset-inject-dedup.mjs:resetDedup` — the companion reset that lets fresh docs re-enter context after compaction.