<!-- auto-generated by systems-registry Pass 1 — do not hand-edit. -->
<!-- Run `tools/systems-registry/cli.mjs hypothesis` to refresh. -->
---
generated_at: 2026-05-29T10:47:48.213Z
generator: claude-opus-4-7
schema_version: 1
---

# Systems hypothesis

I have a comprehensive picture. The repo has the 4 maintainer-specified systems plus two the detector missed: the **installer** (`scripts/`) and **telemetry** (`telemetry/`, written by the hooks). Here's the synthesized map.

```yaml
systems:
  - name: docgen
    kind: pipeline
    summary: >
      Walks a repo deep-first and writes one semver-versioned AUTO_DOCS.md per
      directory by feeding each dir's contents to `claude -p`; triggered by a
      human, cron, or the refresh hook, its output is AUTO_DOCS.md files written
      back into the host repo. Validators (validate-readme / validate-all) gate
      output quality and default to `claude -p` but accept any DOCGEN_JUDGE_CMD.
    globs:
      - docgen/docgen
      - docgen/docgen.mjs
      - docgen/validate-readme.mjs
      - docgen/validate-all.mjs
      - docgen/prompt.md
      - docgen/package.json
      - .docgen/**
      - "**/AUTO_DOCS.md"
    closes_loop_via: host_repo — writes/updates per-directory AUTO_DOCS.md files
    consumes: []

  - name: systems-registry
    kind: pipeline
    summary: >
      Detects cross-file "systems" then runs a multi-pass LLM pipeline
      (detect → pass1 hypothesis → pass2 body → pass2.5 vet → pass2.6 revise →
      validate/refine → organize → build-static) to emit per-system manifests
      under docs/systems/ with mermaid loops, anchors, and invariants, plus a
      static HTML viewer. Run by a human/CLI or the refresh hook; output is the
      docs/systems/*.md manifests + the rendered site.
    globs:
      - systems-registry/cli.mjs
      - systems-registry/detect.mjs
      - systems-registry/pass1.mjs
      - systems-registry/pass2.mjs
      - systems-registry/pass25-vet.mjs
      - systems-registry/pass26-revise.mjs
      - systems-registry/refine.mjs
      - systems-registry/validate.mjs
      - systems-registry/validate-all.mjs
      - systems-registry/validate-mermaid.mjs
      - systems-registry/run-pipeline.mjs
      - systems-registry/organize.mjs
      - systems-registry/composite.mjs
      - systems-registry/registry.mjs
      - systems-registry/scaffold.mjs
      - systems-registry/repo-meta.mjs
      - systems-registry/refresh.mjs
      - systems-registry/llm-retry.mjs
      - systems-registry/build-static.mjs
      - systems-registry/view.mjs
      - docs/systems/**
    closes_loop_via: host_repo — writes docs/systems/*.md manifests + static site
    consumes: []

  - name: context-injection
    kind: hook
    summary: >
      PreToolUse hooks that fire when Claude is about to Read/Edit/Write/Glob/Grep
      a file: docgen's hook injects the nearest AUTO_DOCS.md map and systems-registry's
      hook injects any matching system manifest's mermaid loop into the next LLM turn,
      with per-session dedup so each page emits once.
    globs:
      - docgen/inject-readme-context.mjs
      - systems-registry/inject.mjs
      - hooks/hooks.json
      - hooks/settings.json
    closes_loop_via: next_llm_turn — emits AUTO_DOCS + system mermaid on stdout into Claude's context
    consumes:
      - docgen
      - systems-registry

  - name: auto-docs-refresh
    kind: hook
    summary: >
      PostToolUse(Bash) hook that detects `gh pr create` / `git push` of a feature
      branch, diffs the branch against origin/main, runs scoped docgen +
      systems-registry refreshes in the background, and commits any doc changes back
      onto the branch so fresh docs ride the squash-merge. Paired with reset-inject-dedup,
      which wipes inject dedup state on PreCompact/SessionStart so injection re-fires.
    globs:
      - hooks/auto-docs-refresh.mjs
      - hooks/reset-inject-dedup.mjs
    closes_loop_via: host_repo — commits refreshed AUTO_DOCS + docs/systems back onto the branch
    consumes:
      - docgen
      - systems-registry

  - name: installer
    kind: installer
    summary: >
      One-shot setup script run from a host repo: verifies it's a git repo,
      symlinks/copies the plugin into tools/codecontext/, idempotently merges hook
      entries from hooks/settings.json into the host's .claude/settings.json, ensures
      docs/systems/ exists, and drops a tools/docgen/ shim. --uninstall reverts it.
    globs:
      - scripts/install.sh
      - scripts/merge-settings.mjs
      - scripts/safe-pr-create.sh
      - hooks/settings.json
      - plugin.json
      - .claude-plugin/**
    closes_loop_via: none — one-shot installer, wires hooks then exits
    consumes: []

  - name: telemetry
    kind: data-product
    summary: >
      Owns the machine-wide ~/.claude-telemetry/ append-only JSONL store: the
      inject and refresh hooks call writeEvent() at their recording points, and
      the CLI viewer serves per-session / per-hook / top-files rollups of context
      bytes injected across every repo and session.
    globs:
      - telemetry/writer.mjs
      - telemetry/cli.mjs
    closes_loop_via: dashboard — CLI viewer serves the ~/.claude-telemetry/ JSONL rollups
    consumes:
      - context-injection
      - auto-docs-refresh
```

## Notes

- **Promoted two systems the detector missed.** `installer` (`scripts/`) and `telemetry` (`telemetry/`) were scored 0 / invisible to Pass 0 but are real: the installer is a hook-wiring one-shot, and telemetry is a queryable data-product written by the hooks and served by its own CLI. Promoting them satisfies the coverage check for the `scripts/` and `telemetry/` top-level dirs.
- **`reset-inject-dedup.mjs` placement.** I kept it in `auto-docs-refresh` per the maintainer's explicit file list. Worth a sanity-check: functionally it serves the **context-injection** hooks (it wipes *their* per-session dedup files on PreCompact/SessionStart so injection re-fires). It's coupled to context-injection, not refresh — the only thing tying it to auto-docs-refresh is the `hooks/` directory. Consider moving it if you carve by function rather than by directory.
- **Validators folded, not split.** `docgen/validate-readme.mjs` and `systems-registry/validate.mjs` are gates, but I folded them into their parent pipelines (as the maintainer's carving implies) rather than emitting standalone `gate` systems.
- **Evals excluded** per the explicit instruction — `systems-registry/evals/**` and the three Pass-0 fixture candidates are the test harness, not product systems.
- **Test files** (`*.test.mjs`) are omitted from globs throughout; they shadow their implementation files and add no coverage.
