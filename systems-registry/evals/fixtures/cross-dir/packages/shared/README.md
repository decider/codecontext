<!-- auto-generated by docgen — do not hand-edit. Run `tools/docgen/docgen` to refresh. -->
<!-- docgen:version=0.1.0 reason: initial README for fixture shared package -->

## Purpose
Test fixture for the systems-registry cross-directory eval harness; provides a tiny shared types package that other fixture packages import from to exercise cross-package reference resolution.

## Map
- `Event` interface (id + opaque payload) → `event.ts` · `Event`

## Gotchas
- This is a fixture, not production code — don't refactor it for "real" type safety (e.g. tightening `payload: unknown`). Tests assert against this exact shape.
